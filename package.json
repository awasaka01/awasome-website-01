{
	"name": "awasomewebsite",
	"version": "1.0.0",
	"description": "9",
	"main": "index.js",
	"scripts": {
		"watch:sass": "sass --watch src:__dist/ --style=compressed --no-source-map",
		"build:sass": "sass src:__dist/ --style=compressed --no-source-map",
		"watch:eleventy": "eleventy --serve",
		"build:eleventy": "eleventy",
		"clean": "rimraf __dist",
		"start": "npm-run-all clean --parallel build:sass watch:*",
		"build": "npm-run-all clean build:*"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"dependencies": {
		"@11ty/eleventy": "^3.0.0",
		"eleventy": "^1.0.7",
		"npm-run-all": "^4.1.5",
		"rimraf": "^6.0.1",
		"sass": "^1.80.6"
	}
}